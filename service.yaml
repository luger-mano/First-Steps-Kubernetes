apiVersion: v1
kind: Service
metadata:
  name: mydeployment
  labels:
    app: mydeploylabel
spec:
  selector:
    app: mydeploylabel
  ports:
    - port: 8080
      targetPort: 80
      protocol: TCP
  type: ClusterIP